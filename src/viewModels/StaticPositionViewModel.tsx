import { View, Text } from 'react-native'
import React from 'react'
import StaticPositionScreen from '../views/StaticPositionScreen'

const StaticPositionViewModel = () => {
  return (
    <StaticPositionScreen/>
  )
}

export default StaticPositionViewModel