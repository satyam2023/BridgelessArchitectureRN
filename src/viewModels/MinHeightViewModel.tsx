import { View, Text } from 'react-native'
import React from 'react'
import MinHeightScreen from '../views/MinHeightScreen'


const MinHeightViewModel = () => {
  return (
    <MinHeightScreen/>
  )
}

export default MinHeightViewModel